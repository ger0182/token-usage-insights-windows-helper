name: PowerShell Syntax Check

on:
  push:
    paths:
      - "**/*.ps1"
      - ".github/workflows/powershell-check.yml"
  pull_request:
    paths:
      - "**/*.ps1"
      - ".github/workflows/powershell-check.yml"

jobs:
  parse:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Parse with Windows PowerShell 5.1
        shell: powershell
        run: |
          $failed = $false
          Get-ChildItem -Path . -Filter *.ps1 -Recurse | ForEach-Object {
            $tokens = $null
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile(
              $_.FullName,
              [ref]$tokens,
              [ref]$errors
            ) | Out-Null

            if ($errors.Count -gt 0) {
              $failed = $true
              Write-Host "::error file=$($_.FullName)::Windows PowerShell 5.1 parser errors found"
              $errors | ForEach-Object { Write-Host $_.Message }
            }
            else {
              Write-Host "PS5.1 OK: $($_.FullName)"
            }
          }

          if ($failed) {
            throw "Windows PowerShell 5.1 syntax check failed."
          }

      - name: Parse with PowerShell 7
        shell: pwsh
        run: |
          $failed = $false
          Get-ChildItem -Path . -Filter *.ps1 -Recurse | ForEach-Object {
            $tokens = $null
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile(
              $_.FullName,
              [ref]$tokens,
              [ref]$errors
            ) | Out-Null

            if ($errors.Count -gt 0) {
              $failed = $true
              Write-Host "::error file=$($_.FullName)::PowerShell 7 parser errors found"
              $errors | ForEach-Object { Write-Host $_.Message }
            }
            else {
              Write-Host "PS7 OK: $($_.FullName)"
            }
          }

          if ($failed) {
            throw "PowerShell 7 syntax check failed."
          }
